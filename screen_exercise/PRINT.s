SSISR		EQU 0X4000800C
SSIDR		EQU 0X40008008			
			AREA 	routines, CODE, READONLY
			THUMB
			EXPORT 	PRINT
			
PRINT		PROC			
			LDR		R1, =SSISR
START		LDR		R0, [R1]
			ANDS	R0, R0, #0X3
			BEQ		START
			LDR		R1, =SSIDR
			STR		R5, [R1]
			LDR		R1, =SSISR
CHECK		LDR		R0, [R1]
			ANDS	R0, R0, #0X11
			BNE		CHECK
			BX		LR
			ENDP
			ALIGN
			END